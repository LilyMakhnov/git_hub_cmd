git add exemple.file : indexation of exemple.file
git commit -m "creation project" : creating a new version
git remote add origin https://github.com/OWNER/REPOSITORY.git : adding a new remote with the name origin and a remote URL https://github.com/OWNER/REPOSITORY.git (can use ssh with (ssh-keygen for generating a key).
git remote -v : Verify new remote
git remote set-url origin git@github.com:OWNER/REPOSITORY.git (change url, here updating with ssh))
git push origin main : send the new version to the remote repository
git branch : show all branch
git branch new_branch :create a new branch with the name new_branch
git checkout new_branch : moving into this new branch

<<<<<<< HEAD
git clone git@github.com:LilyMakhnov/git_hub_cmd.git: clone a repository into a new directory
=======

git push --set-upstream origin new_branch : set the new branch as upstream.
>>>>>>> branch_cmd
